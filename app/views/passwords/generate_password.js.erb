// Este arquivo será processado quando a action generate_password for chamada
// Ele atualiza a interface do usuário com a senha gerada

// Encontra o elemento HTML onde queremos exibir a senha gerada
var generatedPasswordElement = document.getElementById('generated-password');

// Atualiza o conteúdo desse elemento com a senha gerada
generatedPasswordElement.innerHTML = "<%= j @generated_password %>";

